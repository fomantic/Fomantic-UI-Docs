semantic.flyout = {};

// ready event
semantic.flyout.ready = function() {

  // selector cache
  var
    // alias
    handler
  ;

  
};


// attach ready event
$(document)
  .ready(semantic.flyout.ready)
;
